# Copy this file to .env and set your values

# MongoDB Atlas connection string (replace with your credentials)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/expenseTracker?retryWrites=true&w=majority

# Port that the backend runs on
PORT=5001

# (Optional) Frontend URL allowed for CORS
CLIENT_URL=http://localhost:5173
